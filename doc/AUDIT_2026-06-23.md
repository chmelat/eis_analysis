# Hloubková analýza projektu EIS Analysis

**Datum:** 2026-06-23
**Auditovaná verze:** 0.13.8
**Verze po opravách:** 0.13.9 (nálezy 2.1, 2.2, 2.3 vyřešeny — viz níže)
**Předchozí audit:** `doc/AUDIT_2026-04-24.md` (v0.13.4–0.13.6)
**Typ:** Automatizovaný statický audit + code-level review (Claude Code)

---

## 0. Co se změnilo od minulého auditu

| Položka | 2026-04-24 (v0.13.6) | Audit (v0.13.8) | Po opravách (v0.13.9) |
|---|---|---|---|
| `pytest` | 75/75 | 75/75 | **78/78** (+3 regresní) |
| `ruff` | čistý | čistý | čistý |
| `mypy` | 110 chyb | 89 chyb | **63 chyb** (−26 v této session) |
| Untracked COMPARISON dokumenty | 4 v `doc/` | **vyřešeno** (pryč) | — |
| `example_data/` nejasnost | otevřeno | **vyřešeno** | — |
| Soubory > 500 ř. | 8 | **9** (mírný růst) | 9 |

Pozitivní trend u mypy a úklidu `doc/`. Naopak **dluh u velikosti souborů
mírně narostl** — `fitting/circuit.py` přerostl z 500 na 516 ř.,
`auto_suggest.py` z 528, `handlers.py` z 840 na 849. Priorita 5 z minula
zůstává nedotčená a tiše zhoršená.

---

## 1. Stav zdravotních metrik (v0.13.9, po opravách)

| Kontrola | Výsledek |
|---|---|
| `pytest tests/` | ✅ 78/78 passed (~10 s) |
| `ruff check eis_analysis/` | ✅ All checks passed |
| `mypy eis_analysis/` | ⚠️ 63 chyb ve 20 souborech (non-blocking) |
| CLI smoke test | ✅ funguje |
| Poměr test/produkce | ~14 % (1 900 / 12 560 ř.) |

**Verdikt:** Projekt je zdravý a zeleně testovaný. Reálná rizika leží
ve **třech** oblastech: (a) netestovaný I/O parser, (b) latentní typové
chyby maskované `except Exception`, (c) rostoucí monolitické soubory.

---

## 2. Nové nálezy na úrovni kódu (priorita tohoto auditu)

### 2.1 `best_start_index` je v paralelním režimu chybný — REÁLNÝ bug — ✅ OPRAVENO (v0.13.9)

`fitting/multistart.py:310`:

```python
best_idx = all_errors.index(best_error) + 1 if best_error in all_errors else 1
```

V paralelním režimu (`multistart.py:283`) se `all_errors` plní v pořadí
**dokončení** (`as_completed`), nikoli v pořadí restartů. Pozice v
`all_errors` proto neodpovídá číslu restartu. Navíc mapování `index + 1`
nesedí na skutečné indexování restartů (`perturbations` startují od `i+1`,
viz `multistart.py:273`, kde `result0` je restart 1).

→ `best_start_index` v diagnostice je v paralelním režimu **zavádějící**.
Dopad je jen diagnostický (ne na výsledek fitu), ale reportovaná hodnota
je nesprávná.

**Oprava (v0.13.9):** přidán seznam `result_indices` zarovnaný s
`all_results`, který drží skutečné `start_idx` každého úspěšného fitu;
vítězný index se čte přímo podle pozice nejlepšího výsledku
(`result_indices[best_pos]`). Odolné vůči přeházení pořadí dokončení i
selhaným restartům. `all_results` zůstal beze změny (veřejný návratový
typ). Regresní testy v `tests/test_multistart_best_index.py` (3 testy);
ověřeno, že proti staré logice paralelní test padá.

### 2.2 `list[float]` drží `None` — křehká diagnostická struktura — ✅ OPRAVENO (v0.13.9)

`multistart.py:290,299` plní `all_errors` hodnotou `None` u selhaných fitů,
ale lokální `all_errors` byl mypy inferován jako `list[float]`
(mypy `arg-type` na ř. 323 vs `MultistartDiagnostics.all_errors:
List[Optional[float]]`). Důsledky:
- `min(...)` / agregace nad `all_errors` by spadly na `None`.
- `best_error in all_errors` (ř. 310) fungovalo jen náhodou.

**Oprava (v0.13.9):** lokální proměnná anotována explicitně
`all_errors: List[Optional[float]] = []` (sladěno s dataclass kontraktem,
který už `Optional` deklaroval). Spotřebitel `best_error in all_errors`
byl navíc opravou 2.1 nahrazen robustním `result_indices[best_pos]`, takže
už na pozicích v `all_errors` nezávisí. Vyčistilo to mypy chyby na
ř. 290/299/323.

### 2.3 Implicit `Optional` defaulty v `diffevo.py` — ✅ OPRAVENO (v0.13.9)

`fitting/diffevo.py:41` — `fixed_params: list = None`,
`full_initial_guess: list = None`. PEP 484 to zakazuje; `None` je
**reálně dosažitelná** hodnota (obvod bez `get_all_fixed_params` nechá
`fixed_params=None`, viz `diffevo.py:206–209`) a propaguje se do
`_DECostFunction`, `make_jacobian_function` i `compute_covariance_matrix`
(ř. 234, 292, 360).

**Upřesnění proti prvotnímu znění:** za běhu to dnes **nespadne** —
všichni tři konzumenti `None` explicitně ošetřují
(`_reconstruct_params:50`, `jacobian.py`, `covariance.py:173,202`).
Jde tedy o **latentní typový dluh**, ne živý pád. Riziko je budoucí:
anotace tvrdí non-optional `list[bool]`, takže (a) mypy nedokáže chytit
skutečné None-misuse v této cestě a (b) budoucí refaktor může strážní
`is None` odstranit s vírou v anotaci → `TypeError: 'NoneType' is not
iterable`, a to jen pro obvody bez `get_all_fixed_params`.

**Oprava (v0.13.9):** anotace na 5 místech ve 3 souborech
(`diffevo.py`, `jacobian.py`, `covariance.py`) změněny na `Optional[...]`,
doplněn import `Optional`. Bez změny chování. Vyčistilo to mypy chyby
`diffevo.py:41/234/292/360`.

### 2.4 `io/data_loading.py` — 650 ř., 12 mypy chyb, NULA testů — ✅ VYŘEŠENO (v0.13.9)

Nejrizikovější modul projektu: parsuje Gamry `.DTA` i CSV, měl 3×
`except Exception` (ř. 68, 151, 337), které spolykaly jakoukoli chybu
parsování a vrátily `None`/částečná metadata. Kombinace **bez jediného unit
testu** + **nejvíc mypy chyb** + **tiché polykání chyb** = ideální místo
pro tichou regresi.

**Opravy (v0.13.9), všechny tři osy nálezu:**
- **Testy:** `tests/test_io_data_loading.py` (29 testů — viz sekce 5).
- **Mypy:** anotace slovníku `metadata: Dict[str, Any]` (jeden kořen všech
  12 chyb) → 0 chyb v modulu.
- **Tiché polykání:** všechny 3× `except Exception` zúženy na
  `except OSError`. IO chyby (chybějící/nedostupný soubor) se stále ošetří
  graceful, ale reálný parsovací bug v `parse_dta_metadata` nově vyplave
  místo tichého návratu částečných metadat. Per-pole guardy
  `(ValueError, IndexError)` uvnitř smyček beze změny. 2 regresní testy
  zamykají chování zúžených exceptů.

### 2.5 Bare `except Exception` v interních cestách — 24× (bylo 27, −3 v io)

Z výskytů jsou v CLI handlerech přijatelné (hezká chybová hláška).
Tři v `io/data_loading.py` zúženy na `OSError` (viz 2.4). Problematické
zůstávají vnořené fallbacky v jádře:

- `drt/core.py:379,389` — dvojitě vnořený `except Exception` kolem výběru
  lambda. Vnitřní fallback spolkne i programátorské chyby (`KeyError`,
  překlep v argumentu) a tiše vrátí `lambda=0.1`. Uživatel netuší, že
  auto-lambda selhala kvůli bugu, ne kvůli datům.
- `drt/peaks.py:98`, `drt/gcv.py:53,120` — stejný vzorec.

Doporučení: zúžit na očekávané třídy (`np.linalg.LinAlgError`,
`ValueError`, `scipy.optimize.OptimizeWarning`) a logovat traceback v
debug úrovni, aby skutečný bug nezmizel.

---

## 3. Mypy — 63 chyb podle typu (po opravách v0.13.9)

| Kategorie | Audit (89) | Po opravách (63) | Poznámka |
|---|---:|---:|---|
| `var-annotated` | 24 | 23 | Chybí anotace u prázdných `[]`/`{}` — kosmetika |
| `assignment` | 24 | 10 | Implicit `Optional` defaulty — část vyřešena (2.3) |
| `arg-type` | 18 | 10 | **Část reálná** — vyřešen `multistart` + `diffevo` |
| `attr-defined` | 9 | 9 | `matplotlib.cm.tab10` apod. — stub/API drift |
| `return-value` | 5 | 5 | Vyžaduje kontrolu |
| `valid-type` | 3 | 3 | `builtins.any` jako typ (překlep za `Any`) |
| `union-attr` | 3 | 2 | None bez guardu — potenciálně reálné |
| `operator` | 2 | 2 | `None / float` — potenciálně reálné |
| `no-redef` | 1 | 1 | |

**Top soubory:** `voigt_chain/fitting.py` (11), `auto_suggest.py` (9),
`drt/gcv.py` (5), `jacobian.py` (4). `io/data_loading.py` (dříve 12) je nyní
**0** (2.4).

V této session vyřešeno 26 chyb (multistart, diffevo/jacobian/covariance
implicit `Optional`, celý `io/data_loading.py`). Zbývá: oddělit
~35 kosmetických (`var-annotated` + `attr-defined`) od ~10 reálných
(`union-attr`, `operator`, zbývající `arg-type` s None, `valid-type`).
Reálné opravit, kosmetické dohnat. Pak zvážit přepnutí mypy v CI z
`continue-on-error: true` na blokující — jinak se dluh bude dál plížit.

---

## 4. Velikost souborů — dluh narostl

| Soubor | v0.13.6 | v0.13.8 | Limit |
|---|---:|---:|---|
| `cli/handlers.py` | 840 | **849** → rozděleno ✅ (v0.13.15) | 500 |
| `drt/core.py` | 810 | 810 | 500 |
| `io/data_loading.py` | 650 | 650 | 500 |
| `fitting/circuit_elements.py` | 647 | 647 | 500 |
| `validation/kramers_kronig.py` | 536 | 536 | 500 |
| `fitting/voigt_chain/fitting.py` | 532 | 532 | 500 |
| `rinf_estimation/rlk_fit.py` | 529 | 529 | 500 |
| `fitting/auto_suggest.py` | 528 | 528 | 500 |
| `fitting/circuit.py` | 500 | **516** | 500 |

9 souborů přes limit (minule 8). `handlers.py` rozdělen (v0.13.15) na balík
`cli/handlers/` po fázích pipeline (validation/rinf/drt/fitting/oxide,
největší submodul 418 ř.); veřejné API zachováno přes re-export v
`__init__.py`. Zbývající kandidát na rozpad: `circuit_elements.py` (rozdělit
dle kategorií elementů: R/C/L, distribuované Q/W/Wo, kompozitní K/G).

---

## 5. Testovací pokrytí — částečně zlepšeno (v0.13.9)

Moduly bez přímého testu — stav po opravách:

| Modul | Řádků | Přímé testy |
|---|---:|:---:|
| `io/data_loading.py` | 650 | **29** ✅ (v0.13.9) |
| `validation/kramers_kronig.py` | 536 | **24** ✅ (v0.13.13) |
| `fitting/diffevo.py` | 480 | **18** ✅ (v0.13.14) |
| `fitting/auto_suggest.py` | 528 | **0** |
| `drt/term_classification.py` | 211 | **0** |

`io/data_loading.py` nově pokryt `tests/test_io_data_loading.py` (27 testů:
DTA/CSV parsery, metadata, OCV; synthetic fixtures + smoke testy proti
`example/EISPOT-test1.DTA`). Ověřeno mutation testem (vypnutí evropského
decimal handlingu shodí testy).

`kramers_kronig` nově pokryt `tests/test_kramers_kronig.py` (24 testů:
pseudo-chi²/noise/reconstruct exaktně, `lin_kk_native` +
`kramers_kronig_validation` na KK-kompatibilních Voigt spektrech, `KKResult`
kontrakt, regrese auto-extend z v0.13.10; ověřeno mutation testem). ✅ (v0.13.13)
`diffevo` nově pokryt `tests/test_diffevo.py` (18 testů: `_DECostFunction`
rekonstrukce/cost/picklability, `DE_STRATEGIES`, recovery na bezšumových
datech, reprodukovatelnost přes nový `seed` param, fixní parametry,
diagnostika; ověřeno mutation testem). ✅ (v0.13.14)

---

## 6. Priority k řešení (seřazeno dle poměru přínos/úsilí)

1. ✅ **Testy `io/data_loading.py`** — vyřešeno v0.13.9
   (`tests/test_io_data_loading.py`, 27 testů).
2. **Opravit reálné mypy chyby** — 🔶 ČÁSTEČNĚ (v0.13.9): hotovo
   `list[Optional[float]]` v `multistart` (2.2), implicit `Optional`
   v `diffevo`/`jacobian`/`covariance` (2.3) a celý `io/data_loading.py`
   (2.4, 12→0). Zbývá ~10 reálných (`union-attr`, `operator`, zbývající
   `arg-type`, `valid-type` `any`→`Any`) a zúžit `except Exception`
   v `drt/core.py:379,389` (vzor zúžení už aplikován na `io/`).
3. ✅ **Opravit `best_start_index`** v paralelním multistartu (2.1)
   — vyřešeno v0.13.9.
4. **Rozdělit `handlers.py` a `circuit_elements.py`** — 🔶 ČÁSTEČNĚ:
   `handlers.py` rozdělen na balík `cli/handlers/` (v0.13.15, 5 submodulů
   po fázích pipeline, ≤418 ř., veřejné API zachováno). `circuit_elements.py`
   ⏳ OTEVŘENO.
5. ✅ **Testy `kramers_kronig` + `diffevo`** — vyřešeno: `kramers_kronig`
   (v0.13.13, `tests/test_kramers_kronig.py`, 24 testů) a `diffevo`
   (v0.13.14, `tests/test_diffevo.py`, 18 testů).

---

## 7. Hlavní sdělení

Projekt je nadále ve výborné kondici pro jednoautorský vědecký nástroj.
Od minulého auditu se zlepšil mypy (110 → 75) a vyřešil nepořádek v `doc/`.
V této session (v0.13.9) byly vyřešeny nálezy 2.1 (reálný bug v paralelním
multistartu, vč. 3 regresních testů), 2.2 a 2.3 (typové dluhy).

**Tři dluhy se ale stále plíživě zhoršují**, protože je CI nehlídá
blokujícím způsobem: (1) velikost souborů roste, (2) test/produkce poměr
klesá, (3) mypy je non-blocking, takže nové chyby přibývají. Nejvýnosnější
další krok je doplnit testy I/O parseru a přitvrdit CI (blokující mypy po
opravě zbývajících ~10 reálných chyb), aby kvalita nebyla jen na disciplíně
vývojáře.
